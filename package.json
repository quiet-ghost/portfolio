{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host",
    "stage": "bun run build && wrangler dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "check": "astro check",
    "lint": "astro check",
    "cf:typegen": "wrangler types"
  },
  "dependencies": {},
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/cloudflare": "^12.6.7",
    "astro": "^5.14.0",
    "typescript": "^5",
    "wrangler": "^4.49.0"
  },
  "packageManager": "bun@1.3.5"
}
